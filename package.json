{
  "private": true,
  "description": "Your project — drop your code into src/",
  "scripts": {
    "office:build": "cd office && npm run build",
    "office:typecheck": "cd office && npm run typecheck",
    "office:lint": "cd office && npm run lint",
    "office:format": "cd office && npm run format",
    "office:format:check": "cd office && npm run format:check"
  }
}
