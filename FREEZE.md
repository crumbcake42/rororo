# Frozen 2026-09-04

Frozen pending harold-and-claude cutover (#11 there). Do not extend `office/`.

Evaluation: `~/projects/last-resort/.research/` (`p2-feature-mapping.md`, `p3-action-plan.md`, `p5-operating-model.md`).

Verdict: replace `office/` with native Claude Code configuration. Keep the two unblock Actions and the persona bodies. After harold ships, the template is extracted from harold's `.claude/` and `.github/workflows/`, not rebuilt here.
